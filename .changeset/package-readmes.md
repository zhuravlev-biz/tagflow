---
"@tagflow/core": patch
"@tagflow/cloudflare": patch
"@tagflow/cli": patch
---

Each package now ships its own README (what it's for, quick-start, zero-dependency footprint, link to the project) and a LICENSE file, so the npm package pages are no longer blank.
