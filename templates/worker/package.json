{
  "name": "tagflow-worker-template",
  "private": true,
  "version": "0.1.3",
  "description": "Standalone Cloudflare Worker for localized Amazon affiliate links — wrangler deploy and done.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "validate": "tagflow validate",
    "check": "tagflow check"
  },
  "dependencies": {
    "@tagflow/cloudflare": "0.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260719.1",
    "@tagflow/cli": "0.3.0",
    "typescript": "7.0.2",
    "wrangler": "4.110.0"
  }
}
